pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Naver_Login_Kotlin_SDK"
include(":shared")
include(":aos-naver")